<div class="list-groups resume-interest-groups">
  {% for content in data[1] %}
    <section class="resume-list-card resume-interest-card list-group">
      <div class="resume-list-card-heading">
        {% if content.icon %}
          <i class="{{ content.icon }}"></i>
        {% endif %}
        <span>{{ content.name }}</span>
      </div>
      <div class="resume-keyword-list">
        {% for item in content.keywords %}
          <span class="resume-keyword">{{ item }}</span>
        {% endfor %}
      </div>
    </section>
  {% endfor %}
</div>
