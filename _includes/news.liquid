<div class="news">
  {% if site.news != blank %}
    {% assign news_size = site.news | size %}
    <div
      class="table-responsive"
      {% if include.limit and page.announcements.scrollable and news_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      <table class="table table-sm table-borderless">
        {% assign news_all = site.news | reverse %}
        {% if include.limit %}
          {% assign news_featured = news_all | where: 'featured', true %}
          {% if news_featured != empty %}
            {% assign news_all = news_featured %}
          {% endif %}
        {% endif %}
        {% if page.lang == 'zh' %}
          {% assign news = news_all | where: 'lang', 'zh' %}
        {% else %}
          {% assign news = news_all | where_exp: "item", "item.lang != 'zh'" %}
        {% endif %}
        {% if include.limit and page.announcements.limit %}
          {% assign news_limit = page.announcements.limit %}
        {% else %}
          {% assign news_limit = news_size %}
        {% endif %}
        {% for item in news limit: news_limit %}
          <tr>
            <th scope="row" style="width: 20%">{{ item.date | date: '%b %d, %Y' }}</th>
            <td>
              {% if item.inline %}
                {{ item.content | markdownify | remove: '<p>' | remove: '</p>' | emojify }}
              {% else %}
                <a class="news-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>
              {% endif %}
            </td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% else %}
    <p>No news so far...</p>
  {% endif %}
</div>
